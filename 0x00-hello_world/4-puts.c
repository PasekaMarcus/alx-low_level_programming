#include <stdio.h>
/**
 * main - a C program that prints exactly the function, followed by a new line.
 * Return: 0
 */
int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
